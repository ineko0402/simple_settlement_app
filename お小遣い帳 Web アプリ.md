スマホ縦画面で使う「お小遣い帳 Web アプリ」の設計を、実際の UI 動きや実装しやすさを意識してまとめてみたよ。シンプルだけど気持ちよく使える構成にしてあるので、プロトタイプにもそのまま使えるはず。

---

# 📱 お小遣い帳 Web アプリ：UI/UX & 機能設計まとめ

---

## 1. 画面構成（スマホ縦画面）

### **① 入力画面（メイン）**
表形式で **日付 / 入金 / 出金** の3列。

```
┌────────┬───────┬───────┐
│   日付    │ 入金     │ 出金     │
├────────┼───────┼───────┤
│ 1/30     │ 1,000   │           │
│ 1/31     │         │ 500       │
│ …        │ …       │ …         │
└────────┴───────┴───────┘
＋ 行追加ボタン
```

- 行は下に追加される
- スクロールで過去分を確認
- ヘッダーは固定

---

## 2. 各セルの入力仕様

### **日付セル**
- タップすると選択肢：
  - 「今日の日付を入力」
  - 「カレンダーから選ぶ」
- カレンダーは OS 標準の date picker を利用（Web の `<input type="date">`）

### **入金 / 出金セル**
- 空欄のとき → タップで数値入力モードへ
- 数値が入っているとき → タップで「残高トースト通知」を表示（後述）

---

## 3. 数値入力 UI（キーボード）

### **基本方針**
- デバイス標準の数字キーボードで OK
- ただし「税込」「割引」「計算」などの補助があると便利

### **選択肢：既存の計算キーボード**
Web では「計算機付きキーボード」は標準では存在しないので、  
**自作のミニ電卓パッドを表示する方式**が現実的。

### **自作ミニ電卓パッド案**
画面下部にスライドアップで表示：

```
┌──────────────────┐
│  7   8   9   +tax │
│  4   5   6   -    │
│  1   2   3   +    │
│      0   .   =    │
└──────────────────┘
```

- 「+tax」：消費税計算（例：1100 → 税抜1000 or 税込1100）
- 「=」：計算結果をセルに反映
- キーボードと併用可能（ユーザーが選べる）

---

## 4. 残高トースト通知の仕様

### **発火条件**
- 入金 or 出金に数値が入っている行で
- 日付 / 入金 / 出金 のいずれかをタップしたとき

### **表示内容**
- その行までの累計残高を計算して表示  
  例：「残高：12,500円」

### **通知方法（おすすめ）**
**Web の Toast UI ライブラリを使うのが簡単で綺麗。**

候補：
- **Toastify.js**（軽量・シンプル）
- **Notyf**（モダンで綺麗）
- **Shoelace の `<sl-alert>`**（Web Components）

**おすすめ：Toastify.js**  
- 1行で呼び出せる  
- スマホでも綺麗に出る  
- カスタム CSS も簡単

---

## 5. 集計画面（サブ画面）

### **タブ切り替え式がスマホ向き**
画面下部にタブバー：

```
[ 入力 ]  [ 日次 ]  [ 週次 ]  [ 月次 ]
```

### **日次集計**
- 日付ごとの入金合計 / 出金合計 / 差額
- 棒グラフ or リスト

### **週次集計**
- 週番号ごとに集計
- 週の開始曜日は設定で変更可能（デフォルト：月曜）

### **月次集計**
- 月ごとの入金・出金・残高推移
- 折れ線グラフが相性良い

### **グラフライブラリ**
- Chart.js（軽量で十分）
- ApexCharts（スマホで見やすい）

---

## 6. データ保存方式

### **最初はローカルで十分**
- LocalStorage
- IndexedDB（大量データでも安心）

### **将来的にクラウド対応したいなら**
- Supabase
- Firebase
- Appwrite

---

## 7. 追加であると便利な機能

- **メモ欄（任意）**  
  → 使いすぎた理由を書ける
- **カテゴリ（食費・娯楽など）**  
  → 集計がもっと便利に
- **CSVエクスポート**  
  → 家計簿アプリに移行しやすい
- **テーマ（ライト/ダーク）**

---

# まとめ

スマホでサクサク入力できて、残高もすぐ確認できる「軽いお小遣い帳」を目指すなら、今回の設計で十分実用的になると思う。

- 表は3列でシンプル
- 日付は date picker
- 入金/出金はミニ電卓パッドで快適入力
- トースト通知で残高を即確認
- 集計画面はタブで切り替え
- データはまずローカル保存

---

必要なら **画面モック（ワイヤーフレーム）** も作れるし、  
**実装用の HTML/CSS/JS の雛形** も作れるよ。  
どこまで作り込みたいか教えてくれたら続けて手伝うね。
